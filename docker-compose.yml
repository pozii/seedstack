services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: seedstack
      POSTGRES_PASSWORD: seedstack
      POSTGRES_DB: seedstack
    ports:
      - "5432:5432"
    volumes:
      - seedstack-postgres:/var/lib/postgresql/data

volumes:
  seedstack-postgres:
